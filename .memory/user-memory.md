# 用户记忆

## 角色
用户当前角色为产品负责人、创始人、项目全栈开发者。

## 技术背景
- 用户熟悉开发概念为 `Java`、`Node.js`、`Python`，有 5 年开发经验，其中 3 年后端、2 年全栈。
- 用户了解架构知识和产品交互体验，所以交互中这部分可以着重和用户讨论

## 工作习惯
- 沟通时偏好一次性集中列出问题，由用户统一拍板。
- 执行前先列计划再动手；进入实现后偏好先整体写完，再根据验证结果和报错逐项修正。
- 开发节奏偏快，后端主要关注数据一致性和状态一致性。
- 每一轮开发结束之后，自动提交git并且push到线上
- 如果用户提示词包含“直接开始无需询问”，则可按命令或计划自主开发；无法自行决策的事项直接写入 `.memory/todolist`。

## 知识水平
- 除非用户直接说明，不需要刻意降低技术密度或做额外照顾；用户看不懂会直接追问。

## 沟通风格（仅适用于对话交互）

- 你是一个平等的工程协作者，不是汇报工作的下属
- 不要为了显得“全面”而重复同一个观点，同一个方案只允许用一种逻辑维度讲一遍
- 禁止使用“结论”、“有以下几点需要说明”、“如果你愿意”、“整体是合理的”等汇报腔/客服腔套话和征询式句式
- 控制层级深度，禁止大段嵌套列表
- 如有明确技术倾向则直接亮明，禁止把论证清楚的决策推回给提问者，如果必须给选项，先亮明你的推荐
- 先给出判断和核心原因，细节按需补充

## 用户偏好

- 2026-04-10 11：用户偏好中文交流，设计文档默认使用中文，implementation plan 默认使用英文，git commit message 使用英文。
- 2026-04-10 16：如果用户给出需求有多种可能，存在模糊，将所有问题集中列出来，每一个问题应该遵守这样格式：”现状、有哪些选择、各自后果、好处和风险、明确建议“，让用户统一回答。
- 2026-04-10 19：每次工作结束时，应自动提交本次工作相关的代码和文件。执行实现计划时不使用 `git worktree`，直接在当前仓库通过本地统一版本管理推进。
- 2026-04-13 09：讨论架构和设计时，术语优先使用全称，避免使用容易歧义的简称；尤其不要把“数据建模定义”和“大语言模型 / 人工智能模型”混称为“模型”。
- 2026-04-18 20：涉及UI/UX或流程讨论时，，默认使用 ascii ui 的方式展示示意
- 2026-04-17 07：以后总结汇报时，主要代码文件需要额外说明两点：这个文件本身负责什么，以及这一轮对这个文件具体做了什么。
- 2026-04-17 08：用户已关闭谷歌浏览器 MCP；后续前端页面的浏览器打开、检查、截图和交互复现默认使用 Playwright，不再主动走 Chrome 浏览器 MCP。
- 2026-04-17 23：如果用户要求调整 UI、修复 UI 或类似样式调整，优先快速完成并给用户看效果；验证时默认只跑对应单元测试或目标测试，不主动跑前端全量测试，把尽快看到调整后效果作为第一优先级。
- 2026-04-21 18：当前项目阶段按开发初期处理，优先完整一致性；默认不考虑兼容问题、回退逻辑和自动迁移，除非用户明确要求。
- 2026-04-21 19：制作计划时候，如果单个plan超过800行,直接做成1+n的plan，一个计划索引+n个执行计划，索引plan 携带 index,子plan以01，02,03
- 2026-05-02 08：执行计划或需要独立 agent / 子 agent 协作时，同一时间只能运行一个独立 agent（不限测试 agent），避免系统资源不足导致宕机；每完成一个计划任务都要同步更新计划文档。
- 2026-05-04 21：push 时，除非用户明确要求合并推送、同步其他分支或推送到指定分支，否则仅推送当前分支到远程仓库。
- 2026-05-07 09：后端 Rust 数据一致性 / 状态一致性等重型专项门禁默认放到线上 GitHub Actions 跑；除非用户明确要求，不在本地运行这些重型后端门禁，避免污染或重置本地数据。
- 2026-05-10 16：用户偏好工具型页面中的顶部状态行、摘要行和轻量操作区保持紧凑透明；不要默认做成高白色卡片，只需要容纳一行状态文字和按钮时，应与页面背景融合。
- 2026-05-11 13：做“昨天/今天热点修改、反复改动、AI 编程效率优化”类复盘时，重点不是给业务代码修复建议，而是抽象出 AI 下次如何避免同类错误，并落到 skills、AGENTS、质量脚本或代码环境规则。
